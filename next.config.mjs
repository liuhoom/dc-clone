/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'ufs.sh',
      },
      {
        hostname: 'rlbgo4z40o.ufs.sh',
      },
    ],
  },
}

export default nextConfig
